# 计算器项目审计与增强建议

审计日期：2026-09-15。对象：`C:\XMWJJ\jisuanqidome` 当前工作区的 Free Pascal / Lazarus Windows 计算器。

## 1. 审计结论

**项目已有清晰的多引擎分层和可运行的基础功能，但目前不宜作为“高精度、专业科学计算器”发布。优先修复计算正确性、错误反馈和输入状态，再开展外观优化与功能扩充。**

本轮重新编译了当前源码，运行原有测试、补充边界测试、窗体控件集成测试、性能探针，并检查发布文件与原生调用。原有核心测试 **44/44** 通过，原有 GUI 模拟测试 **11/11** 通过；补充核心测试 **52 项断言中 25 项失败**，补充 GUI 回归测试 **9 项断言全部复现预期问题**。

这些失败断言是专门选择的风险场景，部分共享根因，部分涉及待明确的产品约定；**不能把失败数直接当作独立漏洞数，也不能把通过率当作项目整体可靠性评分。**

最需要先处理的五类问题：

1. 小于 `1e-14` 的合法非零数被当作零，导致 `1 eV → J`、微小科学量运算失真。
2. 代数模式的一元函数、常数和内存回忆结果在继续输入运算符时会丢失。
3. RPN 连续运算缺少正确的自动堆栈提升；程序员有符号除法、零位旋转等产生错误结果。
4. 换算页吞掉键盘输入，历史回填不生效，除零缺少可靠的界面错误状态。
5. 大角度三角函数和长表达式存在明显阻塞风险。

本次交付只包含审计文档、测试代码、源码快照和证据，不修改应用业务源码，不替用户提交或发布。

## 2. 范围、环境和证据可信度

| 项目 | 本轮范围 |
| --- | --- |
| 审计基线 | HEAD 为 `000335c2fb74de9b4aef27e3fc38b05b9bc6fc92`；工作区已有未提交改动与未跟踪的新引擎，测试以这些当前源码为准，不以 HEAD 旧版本为准 |
| 源码 | `unit1.pas/.lfm`、6 个计算与类型模块、`uThemeManager.pas`、`uCalcButton.pas`、项目入口与构建配置 |
| 工具链 | FPC 3.2.2、安装于 `C:\lazarus` 的 Lazarus；LFM 标记 LCL 3.8.0 |
| 系统 | Windows x64，注册表显示版本 25H2、Build 26200.9445；CPU 为 Intel Core i5-12490F |
| 构建方式 | 在 `audit/2026-09-15/src` 保存源码快照后独立构建；最终脚本从快照目录编译，避免依赖项目根目录的旧 PPU |
| 数据安全 | 使用人工数字测试数据；未复制用户剪贴板内容，未导出个人数据，未对外发送项目文件 |
| 可追溯性 | 保存源码与二进制 SHA-256、构建日志、断言输出及性能探针输出；结束前核对快照源码与原文件一致 |

证据等级：**实测**是编译执行得到的结果；**静态确认**是依据源码路径和状态流判断；**待验证**表示尚缺对应环境或真实交互证据。

### UI 测试边界

已创建真实 LCL 窗体，执行按钮事件、窗体键盘事件、模式切换、`Show` 和消息处理；布局测量使用 96 DPI 的控件实际边界。

Windows computer-use 工具尝试启动审计副本后返回 `launched app did not expose a targetable window`，之后枚举仍未得到可操作的计算器窗口。通过授权的可见启动也未解决工具识别问题。因此：

- 本报告的 UI 结论来自控件集成测试、布局测量和颜色计算；没有把 `.Click` 事件调用称为完整鼠标端到端测试。
- 没有真实桌面截图验收，也没有验证鼠标拖出后松开、快速双击、真实 Tab/Space 焦点流程、读屏器表现。
- 没有实测 125%/150%/200% DPI、跨显示器 DPI 切换、Windows 10 独立系统和 ARM64 环境。
- 启动后工具无法识别窗口的现象保留为环境/启动兼容性待查项，**不据此认定所有用户都会遇到启动故障**。

## 3. 测试结果摘要

| 测试集 | 实际结果 | 说明 |
| --- | --- | --- |
| 原有 `test_calc.lpr` | 44/44 通过 | README 写 41 项，与实际数量不符；覆盖基础路径 |
| 原有 `test_gui_calc.lpr` | 11/11 通过 | 调用按钮事件；主题测试只是比较常量 `'1'` 和 `'1'` |
| 新增 `audit_core.lpr` | 27 通过、25 失败，共 52 项断言 | 包含计算状态、精度、异常、RPN、字长、进制及单位转换 |
| 确定种子的代数验证 | 200 个 `n + 7 × 3` 样例全部正确 | 只证明该表达式形状的优先级，不能泛化为全面随机测试 |
| 单位往返验证 | 10 类、530 个单位对；6 对失败 | 输入固定 `123.456`，失败均涉及 eV 向其它能量单位转换后回转 |
| 新增 `audit_gui.lpr` | 9/9 风险断言复现 | 包括表达式丢项、纸带、换算键盘、除零与键盘异常 |
| GUI 最小尺寸布局 | 检出工具栏重叠、程序员控件越界 | 见第 6 节；首次未 Show 的布局结果已排除，最终日志来自 Show 后测试 |
| 进程隔离性能探针 | 大角度与长表达式触发超时或明显延迟 | 超时子进程被终止，不持续占用 CPU |
| HeapTrc 辅助检查 | 原有核心测试退出时报告 1 块、63 字节未释放 | 栈回溯指向测试结尾的 `Format` / `Halt` 路径；尚未证明是计算引擎持续泄漏 |

`200×10%`、`200÷10%`、幂结合方向、字长输入溢出几项断言采用本报告建议的产品规则。它们与能够直接证明的数学错误、状态丢失分开解读，详见 F12。

## 4. 计算逻辑与健壮性问题

级别定义：**P1** 为发布前优先修复的计算错误、主要功能不可用或可由普通输入触发的阻塞；**P2** 为重要的交互、规则、性能和发布保障问题；**P3** 为后续增强。本轮未确认远程代码执行、越权访问等 P0 安全漏洞。

### F01 · P1 · 通用浮点清理破坏合法的小数值【实测】

位置：`uMathUtils.pas:47`，以及代数、RPN、单位换算对 `SanitizeFloat` 的调用。

| 复现输入 | 正确结果 | 实测结果 |
| --- | --- | --- |
| `10^-20` | `1e-20` | `0` |
| `1e-20 × 1` | `1e-20` | `0` |
| `1 eV → J` | `1.602176634e-19` | `0` |
| 普朗克常数经过通用清理函数 | `6.62607015e-34` | `0` |

根因是把 `abs(value) < 1e-14` 全局映射为零。这一策略可以掩盖某些三角函数残差，却不适用于科学量、指数运算和单位转换。

建议：取消通用清零；只对已识别的特殊角度做局部处理。保留 IEEE 754 双精度值，负零的显示归一化独立处理。科学常数与单位换算使用相对误差验收，避免 `1e-6` 这类绝对容差把微小值错误也判为通过。

验收：上述四项保持非零；530 个单位往返用例全部通过，额外覆盖 `1e-30`、`1e-15`、`1e15` 的数量级。

### F02 · P1 · 一元函数、常数和 MR 结果被下一运算符覆盖【实测】

位置：`uAlgebraicEngine.pas:203` 的 `InputOperator`，以及 `ApplyUnary`、`InputPi`、`MemoryRecall`、`InputCloseParen`。

| 按键序列 | 期望 | 实测 |
| --- | --- | --- |
| `2 + 9 √x + 4 =` | `9` | `6` |
| `2 × π + 1 =` | 约 `7.28318530718` | `3` |
| `2 + MR(7) + 1 =` | `10` | `3` |
| `( 9 √x ) =` | `3` | `0` |

这些操作把 `FIsNewInput` 设为 True，后续逻辑误以为没有新的操作数，替换前一个运算符或跳过入栈。一个布尔值同时代表“下一位数字应覆盖显示”和“当前操作数尚未完成”，含义冲突。

建议：建立明确的输入状态，例如“正在输入数字、已准备好操作数、等待操作数、刚完成计算、错误”。一元结果、MR、常数、粘贴值都成为有效操作数；下一个数字是否覆盖显示应由另一个状态决定。

### F03 · P1 · RPN 运算后的数字输入缺少堆栈提升【实测】

位置：`uRPNEngine.pas:169`、`:223`。

`3 ENTER 4 + 5 ×` 应得到 `35`，实测得到 `0`。第一次 `+` 得到 7，但输入 5 直接覆盖 X，未把 7 提升到 Y。

建议：单独记录 stack-lift 状态。ENTER 后输入数字覆盖复制出的 X；二元运算或一元运算完成后开始新数字时，按选定的 RPN 规则提升堆栈。明确 T 寄存器下坠后的处理规则，补齐四层内容的断言，而不只断言 X。

### F04 · P1 · 程序员有符号除法实际按无符号执行【实测】

位置：`uProgrammerEngine.pas:159` 的 `ExecuteEquals` 与 `GetDecStr`。

8 位 Signed 模式，位模式 `0xF8` 表示 -8，除以 2 应显示 -4，实测显示 124。`IsSigned` 只改变显示，计算仍对 `QWord` 执行 `div`；余数同样需按所选符号语义处理。

建议：按字长进行符号扩展，对有符号除法和余数采用统一规则。明确最小负数除以 -1 的溢出策略；逻辑右移与算术右移应分别命名，避免 Signed 模式让用户误解当前 Rsh。

验收：8/16/32/64 位分别覆盖正负数混算、负余数、除零和最小负数边界。

### F05 · P1 · 零位/整字长循环移位返回了移位量【实测】

位置：`uProgrammerEngine.pas:159` 的 RoL/RoR 分支。

8 位下 `0x81 RoL 0` 应保留 `0x81`，实际为 0；`0x81 RoR 8` 应保留 `0x81`，实际为 8。移位量取模为零时没有将结果赋为原操作数 A，沿用了 FValue 中的 B。

建议：旋转分支先以 A 初始化结果，再处理非零余数；穷举 8 位全部 256 个数和 0～16 位旋转量，验证旋转逆操作与周期性。

### F06 · P1 · 排列组合的整数范围小于界面承诺【实测】

位置：`uMathUtils.pas:216`、`:231`。

- `3000000000 nPr 1` 应为 3000000000，实测为 **-1294967296**。IntN 使用 32 位 Integer，转换后溢出。
- `67 nCr 33` 的结果约 `1.42265207376203e19`，Double 能表示其近似值，但 `Round(Res)` 先转成 Int64，实测抛出 `EInvalidOp`。
- 巨大组合参数探针抛出 `EOverflow`，不是无限运行；应与三角函数超时问题区分。

建议：先校验输入是否有限、是否整数、是否在可接受范围，再执行类型转换；不为“去掉小数”而把巨大 Double 转 Int64。明确返回“近似浮点结果”还是“精确整数”，并限制计算成本。后续如增加大整数支持，单独设计精确组合算法。

### F07 · P1 · 换算页无法正常使用键盘输入【集成实测】

位置：`unit1.pas:2039`、`:2105`。

在 `cmConverter` 模式，数字、小数点、退格和 Ctrl+V 的按键参数都被改为零，但处理器又没有把它们交给换算输入框。设置换算输入框为 `abc` 后，转换逻辑还通过 `StrToFloatDef(...,0)` 把非法内容当作零。

建议：当焦点位于可编辑文本控件时，优先保留控件原生编辑行为；只在计算模式且没有编辑框消费事件时处理计算快捷键。换算输入使用 `TryStrToFloat`，区分未完成输入、有效输入和非法输入，显示局部错误提示。

验收：在换算框手动输入 `12.5`、退格、选区替换、粘贴科学计数法、输入负温度均正常；非法文本不显示一个看似有效的转换结果。

### F08 · P1 · 除零失败没有可靠的界面错误状态【实测】

位置：`uAlgebraicEngine.pas:389`、`unit1.pas:1528`、`:1429`、`:2105`。

- `1 ÷ 0 =` 的引擎返回 False，但 `CurrentInput` 仍为 0；界面忽略输出参数里的 Error，继续显示 0。
- 此后再次执行等号，保存的重复操作仍为除零，异常向调用者传播。
- RPN 鼠标操作的错误路径有捕获，但键盘 `/` 直接调用引擎，除零异常逸出窗体处理器。未断言整个应用必然退出；LCL 可能显示框架级异常对话框。

建议：用统一命令分发入口处理鼠标和键盘；返回带错误类型的结果，进入明确 Error 状态，清理不再有效的重复运算信息。错误应可见、可恢复，并保留足够上下文供用户修正。

### F09 · P1 · 大角度三角函数存在主线程阻塞风险【实测＋静态确认】

位置：`uMathUtils.pas:85`、`:108`，由代数/RPN 的同步一元运算调用。

DEG 模式对角度反复加减 360，而不是使用有界范围缩减算法。`sin(1e12°)` 和 `sin(1e23°)` 的进程探针均超过 3 秒而被终止。对 `1e23` 这样的 Double，减去 360 已不足以改变原值，因此循环还存在无进展条件；`cos` 使用同类实现，`tan` 间接调用两者。

建议：先拒绝 NaN/Infinity，再采用不会按角度大小线性循环的范围缩减；对超大输入明确精度边界。不要只把循环搬到线程里掩盖算法缺陷。成本不确定的运算再配合取消机制和预算限制。

### F10 · P1 · 显示字符串参与后续运算，丢失可保留的精度【实测】

位置：`uMathUtils.pas:278` 与 `uAlgebraicEngine.pas:389`。

`1000000000000001 + 0 =` 后再 `- 1000000000000000 =`，应得到 1，实际得到 0。该整数本来可被 Double 精确保存；但科学计数法仅输出 10 位有效数字，并写回 CurrentInput，后续从显示串重新解析。

建议：引擎保留数值结果，显示串只负责呈现，不作为结果存储。用户手工输入文本与引擎结果分别管理；提供有效位数或“完整精度复制”选项。当前实现属于双精度浮点计算，不宜宣称任意精度或精确十进制。

### F11 · P2 · 历史记录、回填和常数代入存在状态分支错误【实测／静态确认】

位置：`unit1.pas:1528`、`:1415`、`:1993`、`:1959`。

- **实测**：`2 + 3 =` 的纸带写成 `2 + = 5`。原因是计算前缓存的表达式尚未包含末尾操作数。
- **实测**：先算出 5，再把主显示改为 9，进入纸带回填 5，返回科学模式后仍显示 9。回填函数按 `FCurMode` 匹配 Algebraic/RPN，但按钮实际处于 Tape 模式。
- **静态确认**：常数插入按钮位于 Converter 模式，处理器只写 Algebraic/RPN/Programmer 分支，没有 Converter 分支，却仍提示成功并跳转科学模式。
- **静态确认**：手册宣传常数双击代入、历史双击回填，但对应列表未绑定双击事件。

建议：记录“上一次计算模式”，或在工具页明确选择目标；在成功计算后，从完整求值记录写入历史。历史使用结构化字段保存表达式、精确结果、模式、角度制和时间，避免从一行文本中寻找第一个等号来恢复结果。

### F12 · P2 · 运算规则与非法输入需要明确约定【实测】

位置：代数 `ExecutePercent`、`EvaluateTokens`，程序员 `InputOperator` 和 `InputHexDigit`。

| 行为 | 实测 | 建议的产品约定 |
| --- | --- | --- |
| `200 × 10%` | 4000 | 建议为 20；乘除按百分比操作数处理 |
| `200 ÷ 10%` | 10 | 建议为 2000 |
| `200 + 10%` | 220 | 可保留常见商业百分比语义，但要定义复杂表达式中的基数 |
| `2^3^2` | 64 | 采用数学表达式约定时应右结合为 512；若选择连续计算规则，需文档说明 |
| 多余右括号 `) =` | 成功返回 0 | 应拒绝或在输入阶段阻止；不以栈下溢补零 |
| 粘贴 `abc` 后 `+ 1 =` | 1 | 非法数字应报错，不能悄悄变成零 |
| 程序员 `2 + 3 + 4 =` | 7 | 新运算符应结算前一个待定运算，或明确不支持连续表达式并阻止输入 |
| 8 位输入 256 | 0 | 可以采用模运算模型，但直接输入超界时应提示截断/溢出，避免误以为成功录入 256 |

静态检查还发现 MOD 对小数直接 Trunc，`5 MOD 0.5` 会在浮点非零检查后变成整数除零。建议仅接受整数并清楚提示，或单独提供有文档的浮点余数功能。幂、根、NaN/Infinity、科学计数法粘贴都应归入统一输入验证策略。

## 5. 应用性能与资源表现

本节为单机开发环境数据，不是跨设备基准。核心微基准与 UI 事件基准分别测量；构造窗体用时不等同于从启动到可交互的冷启动时间。

| 场景 | 实测情况 | 解读 |
| --- | --- | --- |
| 10,000 次短表达式 `2+3×4` | 最终完整复跑约 141 ms | 基础计算吞吐足够；不包含绘制和真实按键延迟 |
| 窗体创建 | 本轮约 78～141 ms | 仅 `CreateForm`；不能据此证明“毫秒级可交互启动” |
| 200 次模式切换 | 最终完整复跑约 4,407 ms | 已 Show 窗体；含样式更新和控件调用，平均约 22 ms；不是 p95 帧时间 |
| 同上 Pascal 堆变化 | 净变化 0 字节 | 这段循环未见 Pascal 堆持续增长，不代表已排除全部 GDI/系统资源泄漏 |
| 增加 5,000 条历史 | 约 2,843 ms，Pascal 堆增加 412,800 字节 | 增长部分是预期历史存储；原生 ListBox 占用未全部计入 |
| 100 项连续加法输入并求值 | 约几十 ms | 每次输入都重建表达式 |
| 1,000 项连续加法输入并求值 | 约 3 秒，接近早期探针超时界限 | 瓶颈发生在连续输入和字符串重建，不能只优化最终求值 |
| 5,000 项连续加法 | 3 秒探针未完成 | 未运行到最终完成，不外推总耗时 |
| `sin(1e12°)`、`sin(1e23°)` | 3 秒未完成 | 高优先级算法问题，见 F09 |
| 当前 GUI EXE | 26,256,919 字节，约 25.0 MiB | 包含 `.debug_info` 等多个调试节；现有发布文件也约 25 MiB |

精确的最新性能探针值以 `audit/2026-09-15/logs/probes.json` 为准，其中 `ElapsedMs` 包含进程启动等开销；表达式输出中的 `ms` 是程序内部计时。

性能改进顺序：

1. 修复三角函数范围缩减；这是可用性问题，优先级高于微优化。
2. 表达式 Token 容量按块增长，显示表达式增量更新或延迟重建；目前每次输入都遍历全部 Token，累计成本至少呈平方增长趋势。
3. 为表达式长度、括号深度、数字输入和历史数量设置产品上限，并给出可见提示。建议初始上限 1,024 Token、64 层括号、1,000 条历史，再据真实使用调整。
4. 单位表和常数表改为静态只读数据，避免 `GetUnitName/GetUnitCount/ConvertValue` 反复构造动态数组；这属于次要优化。
5. 模式切换只刷新可见页面和变化的控件；合并多次 Invalidate。建立 Debug/Release 配置，将调试信息独立保存后再测发布体积与启动。

建议新增性能验收门槛：常规按键到刷新 p95 < 50 ms、模式切换 p95 < 100 ms、正常启动到可交互 < 500 ms；它们是拟定目标，当前没有完成这些端到端指标的测量。

## 6. UI、可访问性与交互改进

### 已测得的布局问题

在 Show 并处理消息后，以 96 DPI 调整到 440×600：

- 科学模式顶部“纸带”与 DEG 重叠约 4 px。
- 程序员 Signed/Unsigned 控件位于 x=366、宽 80，右缘 446，超出 440 px 父容器。
- BIN 标签自然宽度为 588 px，从 x=16 开始，超出父容器；64 位分组二进制无法完整呈现。
- 科学页可见的 56 个自绘按钮都没有启用 TabStop。全局数字快捷键无法替代主题、模式、角度和特殊功能的键盘访问。

建议采用可换行工具栏和约束布局，窄窗时折叠辅助项；BIN 单独分两行或可横向滚动。给显示区提供完整复制能力，避免仅通过缩小字体容纳任意长度。`LabelExpr` 与 `LabelMem` 从同一 x/y 开始设置范围，内存标记也应预留独立位置，此项需补截图确认。

### 对比度

根据源码 RGB 值按相对亮度公式计算：

| 文本与背景 | 对比度 |
| --- | --- |
| 白字 / 黑、白主题橙色 `#FF9500` | 2.20:1 |
| 白字 / 浅黄主题运算符 `#E1911E` | 2.54:1 |
| 白字 / 浅黄主题等号 `#D78714` | 2.85:1 |

三组均低于大字号文字常用的 3:1 参考线。作为桌面应用设计参考，W3C 给出的普通文字最低对比度为 4.5:1，大文字为 3:1；本报告不据此宣称已经做过完整 WCAG 合规认证。[W3C 对比度要求](https://www.w3.org/WAI/WCAG21/Understanding/contrast-minimum)

建议：橙底使用深色字，或加深橙色以支持白字；为焦点、禁用、按下和选中状态分别检查对比度。自绘按钮补充 Tab 顺序、Space/Enter 激活、可见焦点环、可访问名称和按钮角色；不能只依赖 emoji 表达功能。

### 后续 UI 验收矩阵

| 维度 | 最低验收范围 |
| --- | --- |
| 模式 | 科学、RPN、程序员、换算、纸带，切换后状态明确且一致 |
| 主题 | 黑、白、浅黄；hover/pressed/disabled/focus 四态 |
| 尺寸 | 最小窗口、默认窗口、最大化；内容不重叠、核心操作可见 |
| DPI | 100%、125%、150%、200%；跨显示器移动后重新布局 |
| 输入 | 鼠标、小键盘、主键盘、剪贴板、编辑框原生快捷键 |
| 可访问性 | Tab/Shift+Tab、Space/Enter、系统高对比度、Narrator 可读名称 |
| 错误恢复 | 除零、非法粘贴、超长表达式后，CE/AC/撤销可恢复工作 |

## 7. 安全性与发布工程

### 已确认的攻击面与限制

当前业务源码未发现主动网络请求、脚本求值、外部命令执行、凭据存储或用户文档写入路径；输入主要来自按钮、键盘和剪贴板。原生外部调用主要是主题管理中的系统 `dwmapi.dll`。这是源码审阅结论，未进行运行期网络抓包，也不是第三方库漏洞库扫描结果。

最现实的风险是：**未经严格验证的数字输入造成静默计算错误或本地资源耗尽**。F09 的超大角度和长表达式已经复现可用性问题，但没有证据支持把它们描述为远程攻击漏洞。

### 发布加固结果【文件实测】

- 当前构建和 `releases/PCalc_Windows_Advanced_Calculator.exe` 的 Authenticode 状态均为 `NotSigned`。
- PE `DllCharacteristics` 为 `0x0000`，没有声明 DynamicBase、HighEntropyVA、NXCompatible 等标志。
- 文件保留大量调试信息；工程只有 Default 构建模式；发布目录存在两个内容相同但名称不同的 EXE，缺少统一的版本产物说明。

这些是加固和可追溯性缺口，**不等于已证明可利用的内存漏洞**。特别是不能仅凭 NXCompatible 标志缺失就断言 Win64 进程关闭了 DEP；实际保护还由操作系统策略决定。[Microsoft PE 标志定义](https://learn.microsoft.com/en-us/windows/win32/debug/pe-format)、[Microsoft DEP 说明](https://learn.microsoft.com/en-us/windows/win32/memory/data-execution-prevention)

建议：

1. 建立 Release 构建模式，验证当前 FPC/链接器支持的 ASLR、重定位及其它加固选项；以产物检查为准，不只改配置文本。
2. 统一发布文件名、版本信息、SHA-256 校验和、构建工具链与源码提交标识。面向公开分发时加入代码签名与时间戳。
3. Debug 中开启范围/溢出检查与 HeapTrc；程序员明确允许的模运算单独实现，避免“全局开启溢出检查”破坏预期字长回绕。
4. 剪贴板输入统一走长度限制、合法数字解析和有限值检查；非法字符串不落入值为零的正常路径。
5. 历史目前驻留内存，后续如增加持久化，默认仅在用户目录保存，提供开关、清空和容量限制。不要提前引入不需要的同步、账号或遥测。

## 8. 测试体系需要同步修复

原有 GUI 套件末尾固定 `Halt(0)`；部分“未找到按钮”的分支只打印 FAIL、不增加失败计数。因此未来即使功能坏掉，CI 仍可能显示成功。主题刷新测试比较固定常量，也不能验证配色、主题循环或布局。

建议以以下层次组织测试：

| 层次 | 必须覆盖的行为 |
| --- | --- |
| 纯引擎 | 已知数学结果、边界与非法值；相对误差与绝对误差按数量级组合 |
| 状态序列 | 运算符替换、一元结果继续运算、括号内一元函数、MR/常数/粘贴、重复等号、错误恢复 |
| 属性验证 | 旋转逆操作、字长掩码、单位往返、RPN 四层寄存器与栈提升 |
| GUI 集成 | 同一命令的鼠标与键盘入口一致；工具页到计算页的值传递；编辑焦点优先 |
| 真正端到端 | 实际鼠标和键盘输入、弹窗、DPI、读屏器、完整截图检查 |
| 性能/资源 | 单独进程、超时保护、启动与按键 p95、模式循环、长历史和窗口句柄增长 |
| 发布 | 干净源码构建、所有失败返回非零、产物版本/签名/hash/PE 标志检查 |

补充测试是缺陷复现集，修复前返回 1 属于预期。对百分比、幂结合性和字长溢出，需要先确定规范，再将相关断言作为正式回归要求。

## 9. 改进实施顺序与验收

工期为单名熟悉 Pascal 的开发者的初步估计，需在修复过程中校准；不包含签名证书采购、完整多系统测试环境准备和高精度库迁移。

| 阶段 | 工作内容 | 估计 | 完成标志 |
| --- | --- | --- | --- |
| A：正确性修复 | F01～F10；统一错误类型与输入状态；修复 RPN/程序员语义 | 5～8 个工作日 | 确定性错误用例全部通过；常见路径保持 44/44；鼠标/键盘除零反馈一致；超大角度在约定预算内返回结果或清楚报错 |
| B：基础交互补齐 | 历史/常数回填、百分比与幂规则、换算校验、显示精度分离 | 3～5 个工作日 | 完整表达式可复现；回填确实改变目标引擎；不存在非法输入悄悄变零 |
| C：UI 与性能 | 最小尺寸布局、对比度、焦点/可访问性、增量表达式、历史限额 | 4～6 个工作日 | UI 矩阵通过；记录实际性能分位数，达到第 5 节约定目标 |
| D：发布工程 | CI 失败语义、Debug/Release、忽略构建垃圾、版本/hash/签名与加固检查 | 2～3 个工作日 | 从干净检出可重复执行构建与测试；只有验证过的产物进入 releases |

建议阶段 A 完成前暂缓扩充高级功能，避免把错误状态和显示精度问题传播到更多模式。

## 10. 后续增强建议

| 优先级 | 增强项 | 用户收益与前提 |
| --- | --- | --- |
| 高 | 撤销/重做与可编辑表达式 | 误按后的恢复更自然；建立在明确状态机和结构化表达式之上 |
| 高 | 科学计数法 E/EXP 输入、有效数字设置、完整精度复制 | 改善科学计算流程；先分离数值与显示 |
| 高 | 精确十进制/大整数作为可选计算类型 | 满足金额或超大组合数等不同需求；明确与 Double 科学模式的精度边界 |
| 中 | 结构化纸带、搜索、CSV/文本导出 | 保留模式、角度制与可复算表达式；避免仅保存显示文本 |
| 中 | SI 与 IEC 数据单位并列 | 现有 KB/MB 实际采用 1024 进制，虽有括号说明，仍建议使用 KiB/MiB 并另提供 kB/MB 的 1000 进制；参考 [NIST 二进制前缀](https://physics.nist.gov/cuu/Units/binary.html) |
| 中 | 常数来源、版本与单位说明 | 标注物理常数采用的数据版本；Mach 需说明介质/温度假设，年份已有 365 天标签应保留 |
| 中 | 可配置字长溢出提示、算术右移与位编号 | 程序员模式更适合检验补码和协议字段 |
| 中 | 记住主题、窗口位置、角度制和上次模式 | 减少重复设置；配置写入应有校验与损坏恢复 |
| 低 | 单位组合、复数、矩阵、统计数据集 | 作为稳定版本后的独立扩展；先定义数学语义和测试 oracle |

## 11. 如何查看与重跑证据

证据目录：`audit/2026-09-15/`。

| 文件 | 用途 |
| --- | --- |
| `audit_core.lpr` | 新增核心回归断言、微基准和超时探针入口 |
| `audit_gui.lpr` | 控件事件、键盘、Show 后布局与资源检查 |
| `build-and-test.ps1` | 从源码快照编译并运行原有及新增套件 |
| `run-probes.ps1` | 在独立进程中运行高成本输入并限制时间 |
| `src/` | 审计时的应用源码快照；用于复现旧问题，不会自动跟随今后的源码修改 |
| `logs/baseline-core.txt`、`baseline-gui.txt` | 原有测试结果 |
| `logs/audit-core.txt`、`audit-gui.txt` | 新增断言和布局测量 |
| `logs/probes.json`、`*.err.txt` | 压力探针时长、退出码及异常输出 |
| `logs/heap-core.txt` | HeapTrc 辅助检查，不应被直接解释为持续泄漏 |
| `logs/source-hashes.json`、`binary-hashes.json` | 审计基线与产物哈希 |
| `logs/pe-headers.txt`、`contrast.json` | PE 文件检查和对比度数值 |
| `logs/exit-codes.json` | 完整脚本的测试退出码 |

在项目根目录运行：

```powershell
pwsh -NoProfile -File .\audit\2026-09-15\build-and-test.ps1
```

工具链不在默认位置时使用 `-Lazarus '实际 Lazarus 目录'`；脚本中的 FPC 子目录目前固定为 3.2.2。重跑会更新审计目录日志和生成文件，可能短暂创建测试窗体；不会覆盖项目根目录业务源码。当前缺陷快照预期返回退出码 1。

修复后的回归应另建新快照或把测试纳入正式测试目录，避免覆盖这份已记录哈希的旧基线。UI 截图、多 DPI、真实输入设备、读屏器及发布版冷启动仍应在后续验收中补齐。
