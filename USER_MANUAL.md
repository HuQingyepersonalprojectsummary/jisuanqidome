# 🧮 PCalc 高级科学与工程计算器 - 全功能使用手册

> **版本**：v2.5 (Windows 10 / 11 原生 64 位架构)  
> **适用平台**：Windows 10 (1809 及以上) / Windows 11  
> **运行环境**：独立单文件绿色免安装，无任何外部环境依赖（无需 .NET、无需 WebView2、无需 VC++ 运行库）

---

## 目录 (Table of Contents)
1. [产品概述与快速入门](#1-产品概述与快速入门)
2. [模式一：科学与代数计算模式 (Scientific Mode)](#2-模式一科学与代数计算模式-scientific-mode)
3. [模式二：RPN 逆波兰表达式模式 (RPN Mode)](#3-模式二rpn-逆波兰表达式模式-rpn-mode)
4. [模式三：程序员模式 (Programmer Mode)](#4-模式三程序员模式-programmer-mode)
5. [模式四：单位换算器与物理常数库 (Unit Converter & Constants)](#5-模式四单位换算器与物理常数库-unit-converter--constants)
6. [模式五：仿真纸带历史记录 (Paper Tape)](#6-模式五仿真纸带历史记录-paper-tape)
7. [个性化主题与辅助功能](#7-个性化主题与辅助功能)
8. [全键盘快捷键速查表](#8-全键盘快捷键速查表)
9. [常见问题解答 (FAQ)](#9-常见问题解答-faq)

---

## 1. 产品概述与快速入门

PCalc Windows 是一款专为科研人员、工程技术人员、程序员与学生打造的高性能、高精度专业桌面计算器。

### 1.1 界面布局概览
启动软件后，主界面由上至下划分为三大核心区域：
- **顶部功能与模式导航栏 (Top Bar)**：
  - **模式切换按键**：`[科学]` (代数模式)、`[RPN]` (逆波兰模式)、`[程序员]`、`[换算]` (单位与常数)、`[纸带]` (历史记录)。
  - **辅助控制键**：角度制切换键（`DEG`/`RAD`/`GRAD`）、窗口置顶按键（`📌 置顶`）、主题切换键（`🌙 黑` / `☀️ 白` / `🌾 浅黄`）。
- **专业墨黑高对比度液晶显示区 (Display Area)**：
  - **副显示屏**：位于上方，实时显示当前输入的完整公式、嵌套括号层级以及内存激活标志（`[M]`）。
  - **主显示屏**：大号荧光文字，呈现当前输入数值或最终计算结果。遇到除以零或非法定义时显示醒目的红色 `Error`。
- **动态功能按键矩阵区 (Keypad Area)**：
  - 随当前所选工作模式自动切换呈现对应的功能键盘。

---

## 2. 模式一：科学与代数计算模式 (Scientific Mode)

点击顶部导航栏的 **`[科学]`** 按钮即可进入代数运算模式。

### 2.1 基础四则运算与运算符优先级
- **严格遵从数学优先级**：计算器内置先乘除、后加减的优先级算法。
  - **操作示例**：依次键入 `2` `+` `3` `×` `4` `=`。
  - **计算结果**：正确得出 `14`（而非传统无优先级计算器的 20）。
- **连续链式运算**：
  - 键入 `10` `+` `20` `+` `30` `=`，主屏幕实时滚动累加，得出 `60`。
- **连续按等号（`=`）重复执行**：
  - 键入 `5` `+` `2` `=`，得出 `7`；
  - 此时若再次按下 `=`，系统会自动重复上一次的加 2 动作，变为 `9`；再次按 `=` 变为 `11`。此特性适用于步进累加或等比倍乘。

### 2.2 嵌套括号运算 `(` 与 `)`
- 支持任意层级的嵌套括号，运算顺序由内向外依次求值。
  - **操作示例**：键入 `(` `2` `+` `3` `)` `×` `4` `=`。
  - **计算结果**：先计算括号内的 2 + 3 得 5，再乘以 4，最终输出 `20`。
- **防呆与容错**：如果在未输入左括号时直接键入右括号 `)`，计算器将自动忽略或拦截，防止公式错乱。

### 2.3 百分比计算 `%`
计算器支持两种主流工程百分比计算逻辑：
1. **加减法增减百分比**：
   - 键入 `200` `+` `10` `%`，计算器将计算 200 加上其自身 10%（即 +20），按下 `=` 得到 `220`。
2. **乘除法求比例值**：
   - 键入 `200` `×` `10` `%`，立即求出 200 的 10%，得出 `20`。

### 2.4 角度模式切换与三角函数
点击顶部栏的 **`DEG`** 按键，可在三种角度度量制度之间循环切换：
- **`DEG` (角度制)**：一周为 360°，直角为 90°。
- **`RAD` (弧度制)**：一周为 $2\pi$，平角为 $\pi$。
- **`GRAD` (百分度制)**：一周为 400 grad，直角为 100 grad。

#### 三角函数与零点精度校准：
- 支持 `sin`、`cos`、`tan` 及其反函数 `asin`、`acos`、`atan`。
- **超强零点保护**：在传统计算器中，由于浮点逼近原因，计算 `sin(180°)` 经常出现 `1.2246e-16` 这种恼人的微小残留；本计算器内置精密零点规约器，计算 `sin(180°)`、`cos(90°)` 严格输出干净利落的 `0`。
- **超大角度自适应**：无论是计算 360° 还是 3600000000°（百亿度），均在毫秒内完成规约求值，绝无界面挂起。

### 2.5 换档键 `2nd` 的使用
- 科学键盘左上方配有 **`2nd`** 换档按键。
- 点击 `2nd` 激活时，按键背景亮起，单目按键功能切换：
  - `sin` 变为反三角函数 `asin`；
  - `cos` 变为反余弦函数 `acos`；
  - `tan` 变为反正切函数 `atan`；
  - `sinh`、`cosh`、`tanh` 分别变为对应的反双曲函数 `asinh`、`acosh`、`atanh`；
  - `x²` 变为 `x³`；
  - `√x` 变为立方根 `∛x`。
- 再次点击 `2nd` 即可复原至基础科学按键。

### 2.6 指数、对数与幂运算
- **幂运算 `xʸ`**：支持连乘与多次方。
  - 例如输入 `2` `xʸ` `10` `=`，结果为 `1024`。
  - 支持多重幂的数学右结合性：`2 ^ 3 ^ 2` 自动解析为 $2^{(3^2)} = 2^9 = 512$。
- **开任意次方根 `y√x`**：输入被开方数后按 `y√x`，再输入根指数。例如 `27` `y√x` `3` `=` 得出 `3`。
- **对数函数**：
  - `log`：以 10 为底的常用对数（例如 `log(100) = 2`）。
  - `ln`：以自然底数 $e$ 为底的自然对数（例如 `ln(e) = 1`）。
  - `eˣ`：$e$ 的 $x$ 次幂。

### 2.7 离散数学与高等统计
- **阶乘 `n!`**：
  - 彻底解除了老式计算器人为限制的 12! 上限，支持从 `0!` 一直到高达 `170!` 的双精度极限阶乘运算（例如 `5! = 120`，`170! ≈ 7.257e+306`）。
- **排列数 `nPr` 与组合数 `nCr`**：
  - **排列数（Permutation）**：从 $n$ 个元素中取出 $r$ 个排列，公式 $P(n, r) = \frac{n!}{(n-r)!}$。输入 `5` `nPr` `3` `=`，输出 `60`。
  - **组合数（Combination）**：从 $n$ 个元素中取出 $r$ 个组合，公式 $C(n, r) = \frac{n!}{r!(n-r)!}$。输入 `5` `nCr` `3` `=`，输出 `10`。
- **模运算 `MOD`**：求整数除法的余数。例如 `17` `MOD` `5` `=`，输出 `2`。

### 2.8 独立记忆体系统 (Memory)
屏幕右上角设有独立的硬件级记忆体控制条，彻底取消了传统弹窗，操作无声丝滑：
- **`MC` (Memory Clear)**：清空当前存储的所有内存值，右上角 `[M]` 指示标识消失。
- **`MR` (Memory Recall)**：调出当前内存中保存的数值，直接显示在输入框中并可继续参与公式运算。
- **`MS` (Memory Store)**：将当前主屏幕中的数值覆盖存入内存，右上角点亮 `[M]`。
- **`M+` (Memory Add)**：将当前数值累加到现有内存值中。
- **`M-` (Memory Subtract)**：从现有内存值中扣除当前数值。

---

## 3. 模式二：RPN 逆波兰表达式模式 (RPN Mode)

点击顶部栏的 **`[RPN]`** 按钮即可切换至专业工程逆波兰模式。

### 3.1 什么是 RPN 及其核心优势？
- **传统代数式**：操作符位于两个数值之间，例如 `3 + 4`，对于复合运算必须频繁输入括号 `(3 + 4) × 5`。
- **逆波兰表达式 (RPN)**：操作数在前，操作符在后。例如计算 `(3 + 4) × 5`，在 RPN 中表示为：`3 4 + 5 ×`。
- **核心优势**：
  1. **零括号**：再复杂的公式也无需输入任何括号；
  2. **逐步确认**：每一步运算立即出结果，无需等到按等号才能看到中间值；
  3. **专业高效**：极大减少击键次数，被惠普工程计算器（HP-12C/48G）推崇数十年。

### 3.2 经典 4 级堆栈监视器详解
屏幕上方实时呈现当前 4 个寄存器的数值：
```
[ T ] 0
[ Z ] 0
[ Y ] 0
[ X ] 0  <-- (当前主显示屏呈现的栈顶数值)
```
- **X 寄存器**：栈顶（Top of Stack）。您正在键盘输入的数字或最新一步计算结果即在 X 寄存器。
- **Y 寄存器**：次栈顶。在进行减法、除法等二元运算时，代表被减数或被除数（即计算 $Y - X$ 或 $Y \div X$）。
- **Z 寄存器**：第三级堆栈。
- **T 寄存器**：第四级堆栈顶部。

### 3.3 堆栈操作命令
- **`ENTER` (压栈)**：
  - 将当前 X 中的数值向上推入 Y，原 Y 推入 Z，原 Z 推入 T。完成推栈后，您即可开始键入下一个数字。
- **`DROP` (弹栈)**：
  - 丢弃当前栈顶 X，原 Y 下落至 X，原 Z 下落至 Y，原 T 下落至 Z。
- **`x<>y` (SWAP 交换)**：
  - 极速对调 X 寄存器与 Y 寄存器的数值。常用于在减法或除法前调整先后顺序。
- **`R↓` (Roll Down 滚降)**：
  - 4 级堆栈循环下滚：X 移至 T，Y 移至 X，Z 移至 Y，T 移至 Z。
- **`R↑` (Roll Up 滚升)**：
  - 4 级堆栈循环上滚：X 移至 Y，Y 移至 Z，Z 移至 T，T 移至 X。

### 3.4 实战演练：计算 $(12 + 8) \times (9 - 4) \div 2$
1. 键入 `12`，按 **`ENTER`**（此时 Y=12，X=12）；
2. 键入 `8`，按 **`+`**（计算 12 + 8，X 变为 `20`）；
3. 键入 `9`，按 **`ENTER`**（20 自动上升至 Y，X=9）；
4. 键入 `4`，按 **`-`**（计算 9 - 4，X 变为 `5`，此时次栈顶 Y 依然保留着 `20`）；
5. 直接按 **`×`**（自动提取 Y=20 与 X=5 进行乘法，X 变为 `100`）；
6. 键入 `2`，按 **`÷`**（计算 100 ÷ 2，最终得出 `50`）。
整个过程无需任何括号，中间结果清晰可见！

---

## 4. 模式三：程序员模式 (Programmer Mode)

点击顶部栏的 **`[程序员]`** 按钮进入。该模式专为计算机体系结构、底层驱动开发、逆向分析及网络协议解析量身设计。

### 4.1 四大进制同屏实时联动
显示屏下方常驻 4 个进制实时转换指示器，无需任何多余操作，任何一进制键入均全自动秒级同步：
- **`HEX` (十六进制)**：支持 `0`~`9` 与 `A`~`F` 键入，高位自动补零对齐（如 `0x00000000000000FF`）。
- **`DEC` (十进制)**：呈现当前数值的十进制表示。
- **`OCT` (八进制)**：呈现 `0`~`7` 的八进制数（如 `377`）。
- **`BIN` (二进制)**：按 4 位一个 Nibble 空格分段呈现，一眼识破高低位字节。

点击左侧的 `HEX`、`DEC`、`OCT`、`BIN` 按钮，即可将键盘当前的输入进制切换至对应进制。

### 4.2 硬件级字长切换 (Word Size)
点击界面上的 **`字长切换`** 按钮，可在 4 种硬件总线规格间轮转：
- **`64-bit (QWORD)`**：64 位四字长（0 ~ 18,446,744,073,709,551,615）。
- **`32-bit (DWORD)`**：32 位双字长（0 ~ 4,294,967,295）。
- **`16-bit (WORD)`**：16 位单字长（0 ~ 65,535）。
- **`8-bit (BYTE)`**：8 位字节长（0 ~ 255）。
> **自动溢出截断机制**：当切换至较小字长时，计算器会自动套用硬件掩码，截断丢弃超出高位，精确模拟嵌入式 MCU 寄存器与 CPU 溢出行为。

### 4.3 有符号补码与无符号切换 (Signed / Unsigned)
- 点击 **`符号切换`** 按钮：
  - **无符号 (Unsigned)**：所有高位 `1` 均计入正数绝对值；
  - **有符号 (Signed)**：遵循二补码（Two's Complement）体系，最高位作为符号位。例如在 8-bit (BYTE) 模式下，十六进制 `0xFF` 在有符号模式下精确直观显示为 `-1`。

### 4.4 硬件位逻辑与移位指令
- **基础位逻辑**：
  - `AND`（按位与）：有 0 则 0，全 1 为 1；
  - `OR`（按位或）：有 1 则 1，全 0 为 0；
  - `XOR`（按位异或）：相同为 0，不同为 1；
  - `NOT`（按位取反）：0 翻转为 1，1 翻转为 0。
- **扩展位逻辑**：
  - `NAND`（与非）、`NOR`（或非）、`XNOR`（同或）。
- **位移操作**：
  - `Lsh` (<< 逻辑左移)：所有比特位向高位移动，低位补 0；
  - `Rsh` (>> 逻辑右移)：所有比特位向低位移动，高位补 0；
  - `RoL` (循环左移)：最高位移出后重新灌入最低位（Rotate Left）；
  - `RoR` (循环右移)：最低位移出后重新灌入最高位（Rotate Right）。

### 4.5 64 位交互式点选比特矩阵 (Bit Matrix)
- 键盘中上方整齐排布着从 **`63` 到 `0`** 的 64 个独立比特小按钮。
- **独立点选即时翻转**：
  - 想要将第 3 位（Bit 3）置 1？直接鼠标点击序号为 `3` 的小按钮，该按钮立刻翻转为高亮按下态，十进制主屏立即同步跳变为 `8`，十六进制同步跳变为 `0x08`！
  - 再次点击即可重置为 0。在排查寄存器掩码与外设引脚配置时极其便捷直观。

---

## 5. 模式四：单位换算器与物理常数库 (Unit Converter & Constants)

点击顶部栏的 **`[换算]`** 按钮进入。

### 5.1 10 大类日常生活与工程单位换算
在左侧分类下拉框中，提供了涵盖现代工程技术的全部核心单位分类：
1. **长度 (Length)**：米 (m)、千米 (km)、厘米 (cm)、毫米 (mm)、微米 (μm)、纳米 (nm)、英寸 (inch)、英尺 (ft)、码 (yd)、英里 (mile)、海里 (nmi)。
2. **面积 (Area)**：平方米 (m²)、平方千米 (km²)、公顷 (ha)、市亩、平方厘米 (cm²)、平方英尺 (sq ft)、平方码 (sq yd)、英亩 (acre)。
3. **体积 (Volume)**：升 (L)、毫升 (mL)、立方米 (m³)、美制加仑 (gal)、美制品脱 (pt)、液体盎司 (fl oz)、立方英尺 (cu ft)。
4. **质量 (Mass)**：千克 (kg)、克 (g)、毫克 (mg)、公吨 (t)、市斤、磅 (lb)、盎司 (oz)、克拉 (ct)。
5. **温度 (Temperature)**：摄氏度 (°C)、华氏度 (°F)、开尔文 (K)（内置精密仿射非线性换算）。
6. **速度 (Speed)**：米/秒 (m/s)、千米/时 (km/h)、英里/时 (mph)、节 (knot)、马赫 (Mach)。
7. **时间 (Time)**：秒 (s)、毫秒 (ms)、微秒 (μs)、分钟 (min)、小时 (h)、天 (day)、星期 (week)、年 (365天)。
8. **数据存储 (Data)**：字节 (Byte)、比特 (bit)、KB、MB、GB、TB、PB（严格基于二进制 1024 进位）。
9. **压力 (Pressure)**：帕斯卡 (Pa)、千帕 (kPa)、标准大气压 (atm)、巴 (bar)、毫米汞柱 (mmHg)、磅/平方英寸 (psi)。
10. **能量与功 (Energy)**：焦耳 (J)、千焦 (kJ)、卡路里 (cal)、千卡 (kcal)、千瓦·时 (度 kWh)、瓦·时 (Wh)、电子伏特 (eV)。

#### 换算操作方法：
- 在上方文本框输入数值（支持键盘直接键入、退格、以及 `Ctrl+V` 快速粘贴）；
- 选定源单位与目标单位，下方结果框实时呈现换算结果；
- 点击 **`⇄ 对调`** 按钮，可瞬间交换源单位与目标单位。

### 5.2 16 大高精度物理与数学常数速查库
在换算界面右侧，常驻国际权威 CODATA 推荐物理与数学常数表：
- **真空中光速 $c$**：$299,792,458\text{ m/s}$
- **普朗克常数 $h$**：$6.62607015 \times 10^{-34}\text{ J}\cdot\text{s}$
- **约化普朗克常数 $\hbar$**：$1.054571817 \times 10^{-34}\text{ J}\cdot\text{s}$
- **万有引力常数 $G$**：$6.67430 \times 10^{-11}\text{ m}^3/(\text{kg}\cdot\text{s}^2)$
- **标准重力加速度 $g$**：$9.80665\text{ m/s}^2$
- **玻尔兹曼常数 $k$**：$1.380649 \times 10^{-23}\text{ J/K}$
- **阿伏伽德罗常数 $N_A$**：$6.02214076 \times 10^{23}\text{ mol}^{-1}$
- **基本电荷量 $e$**：$1.602176634 \times 10^{-19}\text{ C}$
- **电子静止质量 $m_e$**：$9.1093837015 \times 10^{-31}\text{ kg}$
- **质子静止质量 $m_p$**：$1.67262192369 \times 10^{-27}\text{ kg}$
- **摩尔气体常数 $R$**：$8.314462618\text{ J}/(\text{mol}\cdot\text{K})$
- **真空介电常数 $\varepsilon_0$**：$8.8541878128 \times 10^{-12}\text{ F/m}$
- **真空磁导率 $\mu_0$**：$1.25663706212 \times 10^{-6}\text{ N/A}^2$
- **圆周率 $\pi$**：$3.141592653589793...$
- **自然底数 $e$**：$2.718281828459045...$
- **黄金分割比 $\phi$**：$1.618033988749894...$

> **一键代入功能**：在列表中选中需要的常数条目，点击 **`代入当前计算器`** 按钮，该物理常量的高精度完整浮点数将立即注入主计算器，免除手动查表敲打科学计数法的繁琐。

---

## 6. 模式五：仿真纸带历史记录 (Paper Tape)

点击顶部栏的 **`[纸带]`** 按钮进入。

### 6.1 电子纸带特性
- **全流程存盘**：每一次按下等号，系统自动在纸带上打上一行带有当前时分秒时间戳的完整记录，例如：
  `[15:21:27] 2 + 3 = 5`
- **一键回填 (Reuse Result)**：
  - 双击某一条历史记录，或者单选后点击 **`回填结果`** 按钮，该历史条目的计算结果将瞬间填入您当前正在使用的计算模式（代数、RPN 或程序员模式）中，供您继续下一步公式计算。
- **剪贴板批量联动**：
  - **`复制选中`**：仅将当前光标所指的一行算式复制至剪贴板；
  - **`复制全部`**：将纸带中的全部算式清单一次性导出至剪贴板，方便直接粘贴至 Word、Excel 或代码注释中形成计算过程备忘。
- **`清空纸带`**：点击一键清空全部历史记录。

---

## 7. 个性化主题与辅助功能

### 7.1 三套精心调配的专业主题
点击顶部栏右上角的主题切换键（`🌙 黑` / `☀️ 白` / `🌾 浅黄`）可无缝切换：
1. **🌙 经典极客黑 (Default Obsidian Black)**：
   - 沉浸深灰黑机身底色，配合赤陶红（Terracotta）高亮操作按键，文字对比柔和，默认启动，夜晚或暗光环境下使用护眼防刺眼。
2. **☀️ 现代浅亮白 (Modern Light White)**：
   - 通透干净的极简浅灰白调，按钮微凸，层次分明，适合日间高亮度办公环境。
3. **🌾 温润浅黄 (Eye-Care Light Yellow)**：
   - 汲取专业代码编辑器 Solarized Light 风格的温暖米黄调，大幅度降低蓝光刺激，适合全天候长时间连续计算。

### 7.2 窗口悬浮置顶 (📌 Stay on Top)
- 点击右上角的 **`📌 置顶`** 按钮，按钮将呈现高亮按下态；
- 此时计算器将恒久悬浮在所有其他软件窗口之上，方便在浏览网页数据、查看图纸或编写代码时同屏对照计算；再次点击即可取消置顶。

### 7.3 智能自适应布局与无障碍支持
- **极限自适应缩放**：本软件支持自由缩放窗体大小，最小支持在 440×600 紧凑微型窗体下运行，界面组件智能压缩间距，0 遮挡、0 溢出。
- **全键盘 Tab 导航与无障碍环 (Focus Ring)**：支持完全脱离鼠标使用键盘操作，所有交互组件均严格符合无障碍辅助标准。

---

## 8. 全键盘快捷键速查表

| 按键 | 功能说明 | 适用模式 |
| :--- | :--- | :--- |
| **`0` ~ `9`** | 键入对应数字 | 全部模式 |
| **`.`** | 键入小数点 | 科学模式、RPN 模式、换算模式 |
| **`+` `-` `*` `/`** | 分别触发加、减、乘、除运算 | 科学模式、RPN 模式、程序员模式 |
| **`Enter` (回车)** | 执行等号计算求值 / RPN 模式下压栈 | 全部模式 |
| **`=`** | 执行等号计算求值 | 科学模式、程序员模式 |
| **`Backspace` (退格键)** | 删除当前输入末尾的一位字符 | 全部模式 |
| **`Escape` (Esc 键)** | 一键全部清空 (相当于 AC 按钮) | 全部模式 |
| **`(` 与 `)`** | 键入左括号与右括号 | 科学模式 |
| **`A` ~ `F` / `a` ~ `f`** | 键入十六进制字母 A 到 F | 程序员模式 (HEX 状态) |
| **`Ctrl + C`** | 将当前主显示屏呈现的数值复制到剪贴板 | 全部模式 |
| **`Ctrl + V`** | 将剪贴板中的纯文本数字粘贴入计算器 | 全部模式 |

---

## 9. 常见问题解答 (FAQ)

### Q1: 为什么有时候按 `sin(90)` 得出的不是 1？
**A**: 请注意观察顶部栏的角度制指示键。如果当前显示的是 **`RAD`**（弧度制），90 会被当成 90 弧度而不是 90 度。点击该按钮切换回 **`DEG`**（角度制），再次计算 `sin(90)` 即可准确得到 `1`。

### Q2: 遇到除以零或者负数开平方时会闪退或弹窗报错吗？
**A**: **绝对不会**。PCalc 采用了非阻塞安全防御机制，当遇到除以零、负数开偶数根等无定义运算时，屏幕仅会醒目显示红色的 `Error`。您只需直接键入新的数字，计算器便会自动清空错误并无缝恢复正常计算。

### Q3: 历史纸带里的数据在关闭软件后还会保留在屏幕上吗？
**A**: 纸带记录在单次软件运行期间长久保存在内存中，支持上千条记录流畅滚动。如果需要将历史记录长期保存，可在纸带页面点击 **`复制全部`**，粘贴保存至记事本或文件中。

### Q4: 如何在单位换算界面自如复制粘贴外部数据？
**A**: 计算器具有智能键盘路由技术。只要焦点位于单位换算器的输入框内，全局快捷键会自动让行，您可以随意按退格、敲数字、或用 `Ctrl+V` 将 Excel/网页里的数值直接贴进换算框。
